import { Text } from '@tarojs/components'
import PageContainer from '../../components/PageContainer'
import './index.scss'

export default function Settings() {
  return (
    <PageContainer>
      <Text className='page-title'>设置页</Text>
    </PageContainer>
  )
}